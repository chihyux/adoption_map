import React from 'react'
import { BoxLoadingImg } from './style/loading'

const Loading = ({ icon }) => {
  return <BoxLoadingImg src={icon} alt="loading icon"></BoxLoadingImg>
}

export default Loading
